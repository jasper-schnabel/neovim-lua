-- zen-mode
require('zen-mode').setup {
  window = {
    backdrop = 1,
    width = 0.7,
    height = 0.8,
    options = {
      signcolumn = 'no',
      number = false,
      relativenumber = false,
    },
  },
}
